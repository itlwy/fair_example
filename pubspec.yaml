name: fair_example
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
#  fair:
#    path: /Users/lwy/code_workspace/flutter/sanfang/fair/fair
  fair:
    git:
      url: https://github.com/itlwy/fair.git
      path: fair

  cupertino_icons: ^1.0.0
  smooth_star_rating: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^1.4.0
  fair_compiler: ^0.2.0

flutter:

  uses-material-design: true

  assets:
    - assets/bundle/
    - assets/image/